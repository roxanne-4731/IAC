import { AppRegistry, Platform } from 'react-native';
import App from './src/App';

export default App;
AppRegistry.registerComponent('AwesomeProject', () => App);
// Platform.OS === 'ios' ? AppRegistry.registerComponent('AwesomeProject', () => App) :
