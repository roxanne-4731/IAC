import {Dimensions} from 'react-native';

export const { width, height } = Dimensions.get('window');
// margin horizontal small
export const mhs = width / 20;
export const TABS_HEIGHT = height / 12;
export const HADIS_HEIGHT = height / 10;
export const HADIS_TOP = (height / 3) + 50;
export const HEADER_HEIGHT = height / 2;